% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spot-order.R
\name{spot-new-order}
\alias{spot-new-order}
\alias{spot_new_order}
\alias{spot_new_market_order}
\alias{spot_new_limit_order}
\title{Create new order}
\usage{
spot_new_order(
  order_type,
  symbol,
  side,
  quantity,
  price = NULL,
  time_in_force = NULL,
  test = FALSE,
  fills = TRUE
)

spot_new_market_order(...)

spot_new_limit_order(...)
}
\arguments{
\item{order_type}{Order type. One of \code{"LIMIT"}, \code{"MARKET"},
\code{"STOP_LOSS"}, \code{"STOP_LOSS_LIMIT"}, \code{"TAKE_PROFIT"},
\code{"TAKE_PROFIT_LIMIT"} or \code{"LIMIT_MAKER"}.}

\item{symbol}{Symbol.}

\item{side}{Trade side. Either \code{"BUY"} or \code{"SELL"}.}

\item{quantity}{Amount to buy or sell.}

\item{price}{Price at which to buy or sell.}

\item{time_in_force}{Time for which order is valid. Options are: \code{"GTC"}
— (Good Til Canceled) order will remain on the book unless canceled;
\code{"IOC"} —    (Immediate Or Cancel) try to fill as much as possible before
it expires; or \code{"FOK"} — (Fill Or Kill) Order will expire if full
order cannot be filled.}

\item{test}{Is this just a test?}

\item{fills}{Whether to returns \code{fills} element.}

\item{...}{Further arguments passed to or from other methods.}
}
\value{
A data frame.
}
\description{
Exposes the \code{POST /api/v3/order/} endpoint.
}
\examples{
\dontrun{
# Create new order (returns a data frame).
spot_new_order(
  order_type = "LIMIT",
  symbol = "LTCUSDT",
  side = "SELL",
  quantity = 1,
  price = 67,
  time_in_force = "GTC"
)

# Test new order creation (returns a Boolean).
spot_new_order(
  order_type = "LIMIT",
  symbol = "LTCUSDT",
  side = "SELL",
  quantity = 1,
  price = 67,
  time_in_force = "GTC",
  test = TRUE
)
}
}
